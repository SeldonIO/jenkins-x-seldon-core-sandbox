buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: seldonio/core-builder:0.4
        stages:
        - name: end-to-end
          steps:
          - command: ls
            name: test-end-to-end
      postBuild:
        replace: true
        steps:
        - command: echo "hello world"
      setVersion:
        steps:
        - name: next-version
          sh: echo hello
