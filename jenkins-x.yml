buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: seldonio/core-builder:0.4
        stages:
        - name: end-to-end
          steps:
          - command: ls
            name: test-end-to-end
      setBuildVersion:
        steps:
        - name: next-version
          sh: echo "ignoring..."
      setVersion:
        steps:
        - name: next-version
          sh: echo "ignoring..."
        - name: set-version
          sh: echo "ignoring..."
        - name: tag-version
          sh: echo "ignoring..."

