buildPack: none
pipelineConfig:
  pipelines:
    release:
      setVersion:
        steps:
          - name: next-version
            sh: "echo hello"
      pipeline:
        agent:
          image: seldonio/core-builder:0.4
        stages:
          - name: end-to-end
            steps:
            - name: test-end-to-end
              command: ls

