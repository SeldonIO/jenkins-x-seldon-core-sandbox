buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: seldonio/core-builder:0.4
        stages:
        - name: end-to-end
          steps:
          - command: ls
            name: test-end-to-end
      setVersion:
        steps:
        - name: next-version
          sh: ls /workspace/source/VERSION
