buildPack: none
pipelineConfig:
  pipelines:
    release:
      setVersion:
        steps:
        - name: next-version
          sh: echo "ignoring..."
        - name: set-version
          sh: echo "ignoring..."
        - name: tag-version
          sh: echo "ignoring..."
      pipeline:
        agent:
          image: seldonio/core-builder:0.4
        stages:
        - name: end-to-end
          steps:
          - command: ls
            name: test-end-to-end

